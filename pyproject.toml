[tool.poetry]
name = "p2oplossingl9"
version = "0.1.0"
description = "A python version of snake"
authors = ["Tessa"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
pygame = "^2.5.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
